"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.findDepPackageSync = exports.findDepPackage = void 0;
const node_fs_1 = require("node:fs");
const promises_1 = require("node:fs/promises");
const node_path_1 = require("node:path");
const walk_up_path_1 = require("walk-up-path");
const dirExists = async (f) => {
    try {
        return (await (0, promises_1.stat)(f)).isDirectory();
    }
    catch {
        return false;
    }
};
const dirExistsSync = (f) => {
    try {
        return (0, node_fs_1.statSync)(f).isDirectory();
    }
    catch {
        return false;
    }
};
const findDepPackage = async (pkgName, parentPath) => {
    // starting from the dirname, try to find the nearest node_modules
    for (const dir of (0, walk_up_path_1.walkUp)((0, node_path_1.dirname)(parentPath))) {
        const nm = (0, node_path_1.resolve)(dir, 'node_modules') + node_path_1.sep;
        // if it's null, then we need the node_modules itself
        // if it's '' then we use node_modules with an extra / on it
        // thisis only relevant when generating the error message, since
        // of course node_modules// is never going to be a valid package.
        const ppath = pkgName === null ? nm : (!pkgName ? nm : (0, node_path_1.resolve)(nm, pkgName)) + node_path_1.sep;
        if (await dirExists(ppath)) {
            try {
                return (await (0, promises_1.realpath)(ppath)) + node_path_1.sep;
                // the direxists stat will avoid almost all throws that could
                // occur here, but just in case.
                /* c8 ignore start */
            }
            catch { }
            /* c8 ignore stop */
        }
    }
};
exports.findDepPackage = findDepPackage;
const findDepPackageSync = (pkgName, parentPath) => {
    // starting from the dirname, try to find the nearest node_modules
    for (const dir of (0, walk_up_path_1.walkUp)((0, node_path_1.dirname)(parentPath))) {
        const nm = (0, node_path_1.resolve)(dir, 'node_modules') + node_path_1.sep;
        // if it's null, then we need the node_modules itself
        // if it's '' then we use node_modules with an extra / on it
        // thisis only relevant when generating the error message, since
        // of course node_modules// is never going to be a valid package.
        const ppath = pkgName === null ? nm : (!pkgName ? nm : (0, node_path_1.resolve)(nm, pkgName)) + node_path_1.sep;
        if (dirExistsSync(ppath)) {
            try {
                return (0, node_fs_1.realpathSync)(ppath) + node_path_1.sep;
                // the direxists stat will avoid almost all throws that could
                // occur here, but just in case.
                /* c8 ignore start */
            }
            catch { }
            /* c8 ignore stop */
        }
    }
};
exports.findDepPackageSync = findDepPackageSync;
//# sourceMappingURL=find-dep-package.js.map