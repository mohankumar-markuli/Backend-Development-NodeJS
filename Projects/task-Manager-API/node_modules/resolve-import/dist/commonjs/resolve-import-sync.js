"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveImportSync = void 0;
/**
 * Exported as `'resolve-import/resolve-import-sync'`
 * @module
 */
const node_fs_1 = require("node:fs");
const node_module_1 = __importDefault(require("node:module"));
const node_path_1 = require("node:path");
const node_url_1 = require("node:url");
const errors_js_1 = require("./errors.js");
const file_exists_js_1 = require("./file-exists.js");
const is_relative_require_js_1 = require("./is-relative-require.js");
const resolve_dependency_export_sync_js_1 = require("./resolve-dependency-export-sync.js");
const resolve_package_import_sync_js_1 = require("./resolve-package-import-sync.js");
const to_file_url_js_1 = require("./to-file-url.js");
const to_path_js_1 = require("./to-path.js");
// affordance for node 16 <16.17 and 18 <18.9
/* c8 ignore start */
if (typeof node_module_1.default.isBuiltin !== 'function') {
    node_module_1.default.isBuiltin = (moduleName) => {
        if (moduleName.startsWith('node:')) {
            moduleName = moduleName.substring('node:'.length);
        }
        return node_module_1.default.builtinModules.includes(moduleName);
    };
}
/* c8 ignore stop */
// It's pretty common to resolve against, eg, cwd + '/x', since we might not
// know the actual file that it's being loaded from, and want to resolve what
// a dep WOULD be from a given path. This allows us to realpath that directory,
// without requiring that the file exist.
const realpathParentDir = (path) => {
    path = (0, to_path_js_1.toPath)(path);
    return (0, node_path_1.resolve)((0, node_fs_1.realpathSync)((0, node_path_1.dirname)(path)), (0, node_path_1.basename)(path));
};
/**
 * Resolve an import URL or string as if it were coming from the
 * module at parentURL.
 *
 * Returns a string for node builtin modules, and a file:// URL
 * object for anything resolved on disk.
 *
 * If the resolution is impossible, then an error will be raised, which
 * closely matches the errors raised by Node when failing for the same
 * reason.
 */
const resolveImportSync = (
/** the thing being imported */
url, 
/**
 * the place the import() would be coming from. Required for relative
 * imports.
 */
parentURL = undefined, options = {}) => {
    // already resolved, just check that it exists
    if (typeof url === 'string' && url.startsWith('file://')) {
        url = new URL(url);
    }
    if (typeof url === 'object') {
        if (!(0, file_exists_js_1.fileExistsSync)(url)) {
            throw (0, errors_js_1.moduleNotFound)(String(url), String(parentURL));
        }
        const rp = (0, node_fs_1.realpathSync)((0, to_path_js_1.toPath)(url));
        return rp !== (0, node_url_1.fileURLToPath)(url) ? (0, node_url_1.pathToFileURL)(rp) : url;
    }
    const pu = parentURL ? (0, to_file_url_js_1.toFileURL)(realpathParentDir(parentURL)) : undefined;
    if ((0, is_relative_require_js_1.isRelativeRequire)(url)) {
        if (!pu) {
            throw (0, errors_js_1.relativeImportWithoutParentURL)(url, parentURL);
        }
        const u = new URL(url, pu);
        if (!(0, file_exists_js_1.fileExistsSync)(u)) {
            throw (0, errors_js_1.moduleNotFound)(url, String(parentURL));
        }
        return (0, node_url_1.pathToFileURL)((0, node_fs_1.realpathSync)(new URL(url, pu)));
    }
    if ((0, node_path_1.isAbsolute)(url)) {
        if (!(0, file_exists_js_1.fileExistsSync)(url)) {
            throw (0, errors_js_1.moduleNotFound)(url, String(parentURL));
        }
        return (0, node_url_1.pathToFileURL)((0, node_fs_1.realpathSync)(url));
    }
    if (node_module_1.default.isBuiltin(String(url))) {
        return String(url);
    }
    // ok, we have to resolve it. some kind of bare dep import,
    // either a package name resolving to module or main, or a named export.
    const parentPath = (0, to_path_js_1.toPath)(parentURL || (0, node_path_1.resolve)((0, node_fs_1.realpathSync)(process.cwd()), 'x'));
    const opts = {
        ...options,
        originalParent: String(options.originalParent || parentPath),
    };
    if (url) {
        return (0, resolve_package_import_sync_js_1.resolvePackageImportSync)(url, parentPath, opts);
    }
    else {
        return (0, resolve_dependency_export_sync_js_1.resolveDependencyExportsSync)(url, parentPath, opts);
    }
};
exports.resolveImportSync = resolveImportSync;
//# sourceMappingURL=resolve-import-sync.js.map