import { stat } from 'node:fs/promises';
import { statSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
const toPath = (p) => typeof p === 'object' || p.startsWith('file://') ? fileURLToPath(p) : p;
export const fileExists = async (f) => {
    try {
        return (await stat(toPath(f))).isFile();
    }
    catch (er) {
        return false;
    }
};
export const fileExistsSync = (f) => {
    try {
        return statSync(toPath(f)).isFile();
    }
    catch {
        return false;
    }
};
//# sourceMappingURL=file-exists.js.map